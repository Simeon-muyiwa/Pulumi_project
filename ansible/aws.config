# AWS Metadata
vpc_id: vpc-12345678
security_group_id: sg-12345678
aws_region: eu-west-2

# Cluster Identity
cluster_name: "{{ base_config.clusterName }}"
cluster_tag: "{{ security_tags.clusterTag(base_config.clusterName) }}"
cluster_id: "{{ CLUSTER_ID | default('uuid-1234') }}"