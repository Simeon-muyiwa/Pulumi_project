name: kube-cluster-deployment
description: A minimal TypeScript Pulumi program
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
  kubernetes:cluster_name: kubeadm_cluster
